"""Evidence-grounded graph construction and validation for GreenFin.

The package deliberately avoids eager imports so each CLI module can also be
executed with ``python -m`` without ``runpy`` loading it twice.
"""
