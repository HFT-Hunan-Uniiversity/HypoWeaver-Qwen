# 交付验证记录

验证日期：2026-09-05。验证对象为本文件所在的交付目录中的实际源码，包含当前未提交的工程修改及打包适配。

| 检查 | 结果 |
|---|---|
| 后端全量测试 | 627项：626通过、1跳过；跳过项需要未随包分发的私有企业面板 |
| 前端测试 | 6个测试文件、69项全部通过 |
| 前端生产构建 | 通过；存在单包超过500 kB的体积提示，不影响构建 |
| Python语法与JSON结构 | 182个Python文件、493个JSON文件、1个JSONL文件校验通过（封存前检查） |
| 三服务接口 | 使用本地ASGI进程内客户端，工作流、知识与执行服务健康接口均返回200 |
| 默认知识样例 | 已验证公开JSONL样例可以载入目录与检索；不是全文知识快照 |
| Docker Compose配置 | 在独立临时配置中生成测试令牌并执行 config --quiet，通过 |
| 城市空气质量案例复算 | 4项断言通过；核心系数最大绝对差约2.40×10⁻¹¹ |
| 碳市场案例复算 | 16/16通过；模型系数最大绝对差约4.98×10⁻¹¹ |
| 系统六项指标 | 基础、完整系统及四个消融分支各30单元，共180单元，与历史评价一致 |
| Qwen-VL结果复核 | 6份正式抽取重新评分；表格裁剪条件3次均47字段全部正确 |
| 执行就绪正负对照 | 缺输入为blocked，输入完整为ready |
| 密钥排查 | 未检出开发机实际API密钥、服务令牌或封存密钥；长密钥形状仅命中测试用虚拟值 |

## 验证边界

本次没有启动Docker引擎，因此没有执行三个镜像的全新构建和容器端到端启动，也没有从空环境重新下载全部依赖。前端测试与构建复用了开发机已安装且与包内前端锁文件对应的依赖；这些依赖不放入ZIP。

统计和后端检查在本机Python 3.12环境完成，其实际依赖版本见 `docs/validation/delivery_audit.json`。该环境与镜像所使用的原工程直接依赖锁文件并非完全相同，不将本次验证描述为镜像冷启动验证。Dockerfile采用原工程锁文件，知识服务附加依赖已固定为本次可用版本。

未发起新的付费Qwen调用。系统指标是基于公开派生计划与逐单元回执的离线复核，未重新验证被移除的受限文献原文。历史实验原始评价文件、公开派生记录和本次复核结果的含义见 `README_REPRODUCE.md`。

## 文件完整性

`PACKAGE_MANIFEST.json` 是本次实际分发文件的SHA-256清单，`SHA256SUMS.txt` 提供文本形式。执行 `python tools/verify_package.py` 可验证。校验程序不把用户之后创建的 `.env`、运行数据库、缓存和 `reproduced/` 计入原交付清单。压缩包另附SHA-256校验文件。
